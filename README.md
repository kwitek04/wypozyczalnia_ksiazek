Aby uruchomić aplikację za pomocą dockera należy w folderze projektu wpisać:
docker-compose up --build

Następnie w przeglądarce należ wpisać:
http://localhost:8080/

Przy pierwszym logowaniu do systemu należy użyć danych:
mail: admin@admin.pl
hasło: admin